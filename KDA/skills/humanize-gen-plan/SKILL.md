---
name: humanize:gen-plan
description: Local workflow entry for the Humanize plan-generation pattern referenced by `kda-kernel-workflow`. Use when converting an inspection draft into a structured executable plan.
---

# KDA Humanize Gen-Plan Skill Entry

The workflow references the `humanize:gen-plan` pattern as an optional planning aid. No local full Humanize skill source was found in this workspace. This entry records the binding and the intended scope:

1. Convert `docs/draft.md` into `docs/plan.md`.
2. Keep candidates independently implementable.
3. Attach validation and promotion evidence to each candidate.

If a full Humanize skill is installed later, this entry can be replaced with or redirected to that source.
